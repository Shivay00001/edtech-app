# android/key.properties.example
# Copy this file to key.properties and fill in your values
# DO NOT commit key.properties to version control

storePassword=YOUR_KEYSTORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=upload
storeFile=../upload-keystore.jks

# To generate a keystore, run:
# keytool -genkey -v -keystore upload-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload

# Follow the prompts and remember your passwords
# Move the generated upload-keystore.jks to android/upload-keystore.jks